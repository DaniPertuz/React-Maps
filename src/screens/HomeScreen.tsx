import { MapView, BtnMyLocation, ReactLogo, SearchBar } from '../components';

const HomeScreen = () => {
    return (
        <>
            <MapView />
            <BtnMyLocation />
            <ReactLogo />
            <SearchBar />
        </>
    )
}

export default HomeScreen;